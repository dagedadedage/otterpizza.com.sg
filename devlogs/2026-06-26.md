# 2026-06-26 Devlog — Otter Pizza

## Promo Strategy Revamp
- Raised tiers from $60/$200/$500 to **$60/$120/$200**:
  - $60: FREE DELIVERY
  - $120: 5% OFF (free delivery implied)
  - $200: 10% OFF (free delivery implied)
- Old $500/15% tier removed as unreachable
- Detailed unit economics analysis modeled across realistic baskets
- All labels simplified — no redundant "+ FREE DELIVERY" on discount tiers

## Promo System Refactor
- **No hardcoded tiers**: `calculatePromotions(items, tiers)` now takes dynamic tiers
- Client (CartSummary, OrderSummary): fetches `/api/promotions` with fallback constants
- Server (checkout API): fetches from `prisma.promotion` table
- Single source of truth — admin can change tiers without code deploy
- `PromoBanner` updated to new tier text

## Cart Delivery Fee — Stale State Fix
- Bug: delivery fee wouldn't update when crossing promo thresholds (e.g., $60→free, below $60→DB fee)
- Fix: replaced stale `useState` + `useEffect` pattern with derived `displayFee`
- Now reacts instantly to quantity changes — no page refresh needed

## Cart Nudge + Upsell
- Progress nudge shows at ALL levels (not just first tier):
  - $30 → "+$30 for FREE DELIVERY"
  - $80 → "+$40 for 5% OFF"
  - $130 → "+$70 for 10% OFF"
- Nudge is clickable — scrolls to product suggestion section
- **CartUpsell** component: high-value products sorted by price desc
- Quick-add `+` button on each card, Load More with pagination (8/page)
- Menu API updated: `GET /api/menu?featured=true&page=&limit=`

## Side Cart on Order Page
- Floating pill beside category strip: `🛒 3 items | $54.40`
- Desktop: in same row as category filter tabs
- Mobile: floating pill bottom-right
- Hidden when cart empty
- `MenuPageClient` accepts `rightSlot` prop for extensibility

## Product Management
- Product delete is always soft-delete now
- Deleted products move to **Obsolete** category (auto-created) with `sortOrder=9999`
- Preserves all order history (FK integrity)
- Admin Menu: "Show Obsolete" checkbox toggle to view/hide obsolete items
- Public Order page: filters out Obsolete category + out-of-stock items shown

## Homepage ISR
- Added `revalidate = 60` — featured items update within 1 minute without redeploy

## Admin Orders Table
- Serial # column (page-aware: 1–50, 51–100)
- Date column now shows time (HH:MM)
- Sortable headers: Date, Order #, Customer, Total, Status, Type
- Delete button with confirmation dialog
- Status colors: FULFILLED → dark green (emerald-600), OUT_FOR_DELIVERY + READY → yellow

## Code Review + Fixes (10 findings)
- `metadataBase` typo: `otterpizza.com.sg.sg` → `.com.sg` (critical — broke all OG/twitter)
- `hitpay.ts`: env var non-null assertions → `requireApiKey()` helper with clear errors
- Empty catches in checkout + email now log warnings
- Timeslot now required for BOTH delivery and pickup
- Checkout stock validation + order creation wrapped in Prisma transaction

## UAT Automation
- `scripts/uat-order-flow.sh`: 14 tests (pending, paid, lifecycle, cancel, refund, OOS, invoice)
- `scripts/uat-full-lifecycle.sh`: delivery + pickup lifecycle, 6 email types verified
- All tests pass: webhook processing, status transitions, stock guard, invoice tokens

## Commits (18 today)
- `07f3574` homepage ISR, soft-delete to Obsolete
- `2a24874` hide Obsolete from Order, toggle in Admin Menu
- `f6a2f60` serial #, time, sorting on admin orders table
- `580ab26` fix metadataBase typo
- `c8ce0c7` fix 6 code review findings
- `3f9742b` UAT automation scripts
- `e3c554b` show OOS items, Delete button
- `ca4fc34` status color changes
- `9255e93` fix cart delivery fee stale state
- `7866c0d` cart fee instant recalculation
- `b165f33` new promo tiers $50/$150/$250
- `55586ec` promo tiers from DB, no hardcoding
- `129a839` tiers to $60/$120/$200
- `9263ff4` nudge shows at all levels
- `f5de206` cart upsell + clickable nudge
- `e81730b` side cart on Order page
- `23952e6` floating cart pill beside category strip
- `6fb64b5` devlog
- `fee3283` tighten mobile Order page spacing
