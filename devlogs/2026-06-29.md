# 2026-06-29 Devlog

## Domain Cutover: Wix → Vultr (otterpizza.com.sg)

### Background
- Old Wix site at `otterpizza.com.sg` + `www.otterpizza.com.sg`
- New Next.js site on Vultr (45.77.172.222) at temporary domain `otterpizza.com`
- Both `.com.sg` domains needed DNS cutover from Wix to Vultr

### DNS Changes (Wix DNS Panel)
- Root `otterpizza.com.sg` A record → `45.77.172.222`
- `www.otterpizza.com.sg` CNAME → A record → `45.77.172.222`
- MX records preserved (Google Workspace email)
- Wix site disabled after DNS propagation confirmed

### SEO Migration (Phase 1)
- Fixed domain references: `otterpizza.com` → `otterpizza.com.sg` in `hitpay.ts`, `security.txt`
- Deleted stale `public/robots.txt` and `public/sitemap.xml` (dynamic versions in `src/app/` take precedence)
- Added canonical URL tags (`alternates.canonical`) to root layout and product pages
- Added Google Search Console verification tag (carried from old Wix site: `Bu2fjhVcoR5qLUdvbR8SJGaqfUKJuRQTewwkGBMTVaA`)
- Created `GoogleAnalytics.tsx` component (GA4, gated on `NEXT_PUBLIC_GA_MEASUREMENT_ID`)
- Added homepage `metadata` export (was missing, relied on root layout defaults)
- Added `BreadcrumbList` JSON-LD structured data to product pages
- Updated OG image references to 1200×630 (needs actual image file created)
- Added `poweredByHeader: false` to `next.config.ts`
- Added host-based 301 redirects: `otterpizza.com` → `.com.sg`, `www.otterpizza.com.sg` → `.com.sg`
- Added `NEXT_PUBLIC_GA_MEASUREMENT_ID` placeholder to `.env.example`

### Server Configuration (Phase 2)
- Nginx: separated domain handling into dedicated server blocks
  - `otterpizza.com.sg` → serves site + security headers
  - `www.otterpizza.com.sg` → 301 → `otterpizza.com.sg`
  - `otterpizza.com` + `www.otterpizza.com` → 301 → `otterpizza.com.sg`
  - HTTP → HTTPS for all domains
- SSL: Let's Encrypt cert issued for `otterpizza.com.sg` + `www.otterpizza.com.sg` (expires 2026-09-27, auto-renew)
- Security headers: HSTS (max-age=31536000; includeSubDomains; preload), X-Content-Type-Options, X-Frame-Options, Referrer-Policy
- Nginx config saved to repo at `nginx-otterpizza.conf`

### Email Transport Fix
- Flipped transport priority: Resend first, Gmail SMTP as fallback
- Changed Gmail SMTP sender from `han@otter-group.com` to `info@jidechi-dessert.com`
- Updated `GMAIL_USER` and `GMAIL_APP_PASSWORD` in server `.env`
- App restarted with `--update-env` to pick up new credentials
- Resend still failing because domain `otterpizza.com.sg` not verified (acceptable — sticking with Gmail SMTP)
- UAT order flow: 14/14 passed after email change

### Issues Encountered
- Stale `PRISMA_QUERY_ENGINE_LIBRARY` env var on Windows pointing to non-existent `D:\prisma-engines` (workaround: cleared for build)
- Local build failing with memory/page-file exhaustion (built on Linux server instead)
- DNS propagation delay — user saw Wix page until cache cleared (TTL 3600s)
- www CNAME stubbornly cached on some resolvers (resolved after TTL expiry)
- Resend domain verification not completed (DNS records provided, user chose to stick with Gmail SMTP)

### Verification
- All domain routes confirmed: canonical 200, www→non-www 301, `.com`→`.com.sg` 301, HTTP→HTTPS 301
- Canonical tags, GSC verification, OG tags, JSON-LD structured data all rendering correctly
- Real visitor traffic confirmed in nginx access logs (200 OK)
- Site confirmed working on mobile/cellular
- UAT order flow: 14/14 tests passed
- Email flow: working via `info@jidechi-dessert.com` Gmail SMTP

### Remaining
- Create `public/images/og-default.jpg` (1200×630 social share image)
- Set up Google Search Console property + submit sitemap
- Add `NEXT_PUBLIC_GA_MEASUREMENT_ID` to server `.env` when GA4 property is created
- Add `orders@otterpizza.com.sg` as "Send mail as" alias in `info@jidechi-dessert.com` Gmail settings
